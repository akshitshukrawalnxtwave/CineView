export { getSession, setSession, clearSession, isAuthenticated } from './core/session'
export type { Session } from './core/session'
export { checkCredentials, VALID_EMAIL, VALID_PASSWORD } from './core/credentials'
